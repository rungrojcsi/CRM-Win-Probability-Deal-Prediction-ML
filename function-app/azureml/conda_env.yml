name: crm-predictive
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      # match requirements-ml.txt + the Key Vault secret loader
      - pandas==2.2.3
      - numpy==1.26.4
      - psycopg2-binary==2.9.10
      - scikit-learn==1.6.1
      - shap==0.49.1
      - azure-identity==1.19.0
      - azure-keyvault-secrets==4.8.0
